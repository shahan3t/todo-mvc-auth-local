<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="mx-8">
    <h1 class="text-3xl">Todos</h1>
    <div class="px-6 py-4">
        <div class="font-bold text-xl mb-2">Thing to do</div>
            <ul>
            <% todos.forEach( el => { %>
                    <div class="max-w-sm h-24 rounded overflow-hidden shadow-lg flex items-center">
                    <li class='todoItem align-middle' data-id='<%=el._id%>'>
                        <span class='<%= el.completed === true ? 'completed' : 'not'%>'><%= el.todo %></span>
                        <span class='del bg-transparent hover:bg-red-500 text-red-700 font-semibold hover:text-white py-1 px-1 border border-red-500 hover:border-transparent rounded'>Delete</span>
                    </li>
                    </div>
            <% }) %>    
            </ul>
    </div>
    <h2><%= user.userName %> You have <%= left %> things left to do.</h2>

    <form action="/todos/createTodo" method='POST' class="">
        <input type="text" placeholder="Enter Todo Item" name='todoItem' class="text-gray-700 text-sm font-bold mb-2">
        <input type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
    </form>

    <a href="/logout" class="">Logout</a>
</div>

    <script src="js/main.js"></script>
</body>
</html>